# URL del backend Garmin bridge (default: http://localhost:8787).
# Cambialo solo se esegui il server su un'altra porta o host.
VITE_API_URL=http://localhost:8787

# Porta del backend (letta da server/index.mjs)
PORT=8787
